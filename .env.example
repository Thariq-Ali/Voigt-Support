# Generate a strong random secret key, e.g. with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-please

# Comma-separated list of "username:password" pairs for demo logins (plaintext, hashed at startup).
# In production, use a real identity provider or a database.
SUPPORT_USERS=agent:agent123,support:support123
